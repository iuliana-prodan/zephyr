# Copyright 2025 NXP
# SPDX-License-Identifier: Apache-2.0

description: Address translation support for OpenAMP

compatible: "zephyr,addr-translation"

include: base.yaml

properties:
  ranges:
    type: array
    required: true
    description: |
      Driver and device adresses.
  dma-ranges:
    type: array
    required: true
    description: |
      Driver and device adresses.
  reg:
    required: true
